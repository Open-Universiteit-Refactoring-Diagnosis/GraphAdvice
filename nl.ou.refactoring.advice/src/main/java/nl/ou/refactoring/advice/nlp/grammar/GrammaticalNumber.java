package nl.ou.refactoring.advice.nlp.grammar;

/**
 * Represents a grammatical number.
 */
public enum GrammaticalNumber {
	/**
	 * Singular (one, only, single).
	 */
	SINGULAR,
	
	/**
	 * Dual (two, a pair, a couple).
	 */
	DUAL,
	
	/**
	 * Plural (more than two, many, most, all).
	 */
	PLURAL
}